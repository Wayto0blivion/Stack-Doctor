<idea-plugin>
    <id>com.qrowsolutions.stack-doctor</id>
    <name>Stack Doctor</name>
    <vendor email="qrowsolutions@gmail.com" url="https://github.com/Wayto0blivion/Stack-Doctor">Qrow Solutions</vendor>

    <description><![CDATA[
        <h2>Stack Doctor — Docker Compose service map &amp; network doctor</h2>
        <p>
            Turns a <code>docker-compose.yml</code> into a visual <b>service dependency map</b> and runs
            a set of <b>"doctor" checks</b> for the networking mistakes that waste hours:
        </p>
        <ul>
            <li>Service dependency graph (<code>depends_on</code> / shared networks)</li>
            <li>Ports published to <code>127.0.0.1</code> that won't be reachable from other devices</li>
            <li>Missing healthchecks on services other containers depend on</li>
            <li>Suspicious / non-existent host volume paths</li>
            <li><code>depends_on</code> pointing at services that don't exist, and dependency cycles</li>
            <li>Missing <code>env_file</code> references</li>
            <li>Ports that are <code>expose</code>d internally vs actually published</li>
        </ul>
        <p>Open the <b>Stack Doctor</b> tool window to see the map and the diagnosis for any Compose file in your project.</p>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.1.0</h3>
        <ul>
            <li>Initial release: whole-project Compose service map with dependency layout.</li>
            <li>Ten networking "doctor" checks, surfaced inline and in the tool window.</li>
            <li>Context-aware healthcheck generation (bulk, per-service, and as a quick-fix).</li>
            <li>Map colours now follow the active IDE theme.</li>
        </ul>
    ]]></change-notes>

    <!-- Provided by all JetBrains IDEs -->
    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.plugins.yaml</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow
            id="Stack Doctor"
            anchor="right"
            icon="/icons/toolWindow.svg"
            factoryClass="com.qrowsolutions.stackdoctor.ui.StackDoctorToolWindowFactory"/>

        <notificationGroup id="Stack Doctor" displayType="BALLOON"/>

        <localInspection
            language="yaml"
            shortName="StackDoctorCompose"
            displayName="Docker Compose problems (Stack Doctor)"
            groupName="Docker"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="com.qrowsolutions.stackdoctor.inspection.ComposeDoctorInspection"/>
    </extensions>

    <actions>
        <action
            id="StackDoctor.Refresh"
            class="com.qrowsolutions.stackdoctor.actions.RefreshAction"
            text="Refresh Stack Doctor"
            description="Re-scan the project for Compose files and re-run diagnostics."/>
    </actions>
</idea-plugin>
